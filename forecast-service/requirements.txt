# SliceMatic forecast service (Feature C). Python 3.12 recommended (3.13-compatible pins).
# Version-Safety: re-verify latest patched releases before deploy (PRD §4).
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
scikit-learn>=1.5.1,<2      # cp313 wheels from 1.5.1+
pandas>=2.2.3,<3            # first pandas with cp313 wheels
numpy>=2.1,<3               # cp313 wheels
supabase>=2.7,<3
python-dotenv>=1.0,<2

# Dev/test only:
pytest>=8.3,<9
